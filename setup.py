from setuptools import setup

setup(
    name='messagecleaner',
    version='0.0.2',
    author='LafA',
    description='Чистит сообщение от мусора.',
#    packages=['messagecleaner'],
    install_requires=[
        'rasa',
    ]
)
